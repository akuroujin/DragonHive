using System.Collections.Generic;
using Godot;

class SquareFactory : ITileFactory
{
    public Dictionary<TileCoord, BaseTile> GenerateTiles(Control control)
    {
        Dictionary<TileCoord, BaseTile> tiles = new();
        return null;

    }
}